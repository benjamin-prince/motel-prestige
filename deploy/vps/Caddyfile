# Single domain over HTTPS (automatic Let's Encrypt):
#   https://<domain>/        → frontend
#   https://<domain>/api/... → backend
{$PMS_DOMAIN} {
	handle /api/* {
		reverse_proxy backend:8000
	}
	handle {
		reverse_proxy frontend:3000
	}
}
